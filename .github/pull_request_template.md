<!--
Thanks for contributing to biokea.ai. Please keep the summary short
and factual — the "why" is more useful than the "what".
-->

## Summary

<!-- 1–3 bullets. What changed and why. -->

## Scope

<!-- Check everything this PR touches. -->

- [ ] Page copy or content (`src/pages/*`, `src/data/*`)
- [ ] Components / UI (`src/components/*`)
- [ ] Design tokens / styles (`src/styles/*`)
- [ ] Imagery (`public/assets/images/*`)
- [ ] Agent-facing data (`public/llms.txt`, `src/pages/api/*.json.ts`)
- [ ] Build / CI / deploy (`astro.config.mjs`, `.github/`, `wrangler.toml`)
- [ ] Tests (`tests/**`)
- [ ] Docs (`docs/**`, `README.md`)

## Verification

<!-- At least the first three must be green. -->

- [ ] `npm run check` — 0 errors
- [ ] `npm test` — all pass
- [ ] `npm run test:e2e` — all pass
- [ ] Visually verified on desktop + mobile breakpoints (if UI-touching)
- [ ] Lighthouse didn't regress perf / a11y / SEO on affected pages
- [ ] JSON-LD validates (if structured data changed)

## Screenshots

<!-- For UI / imagery changes, drop before/after screenshots. -->

## Notes for reviewer

<!-- Anything that isn't obvious from the diff. -->
