services:
  web:
    build: .
    ports:
      - '4321:4321'
    volumes:
      - .:/app
      - node_modules:/app/node_modules
    env_file:
      - path: .env
        required: false
    command: npm run dev -- --host 0.0.0.0

volumes:
  node_modules:
