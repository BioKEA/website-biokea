---
import BaseLayout from '@/layouts/BaseLayout.astro';
import Eyebrow from '@/components/ui/Eyebrow.astro';
import CtaBand from '@/components/sections/CtaBand.astro';
import { pipelineStages } from '@/data/pipeline';
import { stringifyJsonLd } from '@/lib/json-ld';
---

<BaseLayout
  title="Soil to Claim — the pipeline"
  description="The six-stage pipeline from raw sample to published, verifiable scientific claim."
>
  <section class="max-w-6xl mx-auto px-6 pt-16 pb-8">
    <Eyebrow>PIPELINE · SOIL|WATER|SPECIMEN TO CLAIM</Eyebrow>
    <h1
      class="mt-3 text-4xl md:text-5xl font-semibold tracking-[-0.025em] leading-[1.05] text-[var(--color-ink)] max-w-[24ch]"
    >
      Six stages. One verifiable chain of custody.
    </h1>
    <p class="mt-5 max-w-[60ch] text-slate-600 leading-relaxed">
      From a raw FASTA or DwC-A (Darwin Core Archive) through to a published, data-tethered
      scientific artifact — every step recorded, every claim re-derivable. The same chain runs under
      the BioKEA molecular sequencing service, so customer samples land with the same verifiable
      output as our own.
    </p>
  </section>

  <section class="max-w-6xl mx-auto px-6 py-8">
    <figure>
      <img
        src="/assets/images/pipeline-flow1.png"
        alt="The BioKEA pipeline: field samples flow through Ingest, Analyze, Draft, Review, Broadcast, and Amplify to become verifiable scientific claims."
        width="2508"
        height="627"
        class="w-full block"
        loading="lazy"
      />
      <figcaption
        class="mt-3 text-center font-mono text-[10px] tracking-[0.14em] uppercase text-slate-500"
      >
        Fig · soil | water | specimen → verifiable claim
      </figcaption>
    </figure>
  </section>

  <section class="max-w-6xl mx-auto px-6 py-8">
    <ol class="space-y-6">
      {
        pipelineStages.map((s) => (
          <li class="grid md:grid-cols-[120px_1fr] gap-6 border-t border-slate-900/10 pt-6">
            <div>
              <div
                data-step-number
                class="font-mono text-sm text-[var(--color-teal)] font-semibold tracking-[0.1em]"
              >
                {s.number}
              </div>
              <div class="font-semibold text-[var(--color-ink)] mt-1">{s.title}</div>
              <div class="font-mono text-[11px] text-slate-500 mt-1">{s.subtitle}</div>
            </div>
            <p class="text-slate-600 leading-relaxed">{s.body}</p>
          </li>
        ))
      }
    </ol>
  </section>

  <section class="max-w-6xl mx-auto px-6 py-12">
    <Eyebrow>BEING BUILT</Eyebrow>
    <h2 class="mt-3 text-2xl font-semibold">BioinfoOS</h2>
    <p class="mt-3 text-slate-600 max-w-[62ch] leading-relaxed">
      The software layer running on the BioKEA Large Data Collider (LDC). In-house AI-assisted
      modules cover:
    </p>
    <ul class="mt-4 space-y-1.5 text-sm text-slate-600 leading-relaxed max-w-[62ch]">
      <li>· Extraction-run QC (Claude Vision over plate images)</li>
      <li>· Taxonomy reconciliation against BOLD, NCBI, and GBIF</li>
      <li>
        · FAIR (Findable, Accessible, Interoperable, Reusable) package validation — DwC-A, Darwin
        Core, Zenodo DOI-ready
      </li>
      <li>· Draft narrative generation tethered to pipeline outputs</li>
      <li>
        · Operational Taxonomic Unit (OTU) clustering, amplicon denoising, and chimera filtering
      </li>
    </ul>
    <p class="mt-4 text-slate-600 max-w-[62ch] leading-relaxed">
      Modules ship incrementally; BioinfoOS is in active development and runs on the same LDC
      hardware used by the molecular sequencing service.
    </p>
  </section>

  <section class="max-w-6xl mx-auto px-6 py-12">
    <Eyebrow>PUBLISHED AT</Eyebrow>
    <h2 class="mt-3 text-2xl font-semibold">Agentis</h2>
    <p class="mt-3 text-slate-600 max-w-[62ch] leading-relaxed">
      Our open-access publishing platform — AI pre-screen plus verified human peer review, with
      outputs as interactive StoryMaps rather than static PDFs. Lives on AT Protocol.
    </p>
    <a href="/agentis" class="mt-4 inline-block font-mono text-sm text-[var(--color-pink)]">
      agentis.science
    </a>
  </section>

  <section class="max-w-6xl mx-auto px-6 py-12">
    <Eyebrow>TRUST</Eyebrow>
    <h2 class="mt-3 text-2xl font-semibold">Verifiable, open, resistant to capture</h2>
    <p class="mt-3 text-slate-600 max-w-[62ch] leading-relaxed">
      Every artifact carries an AT Protocol DID (Decentralized Identifier). Every peer review is a
      signed record. Data packages land in GBIF, NCBI SRA (Sequence Read Archive), and Zenodo. FAIR
      compliance is not optional — it is the output format.
    </p>
  </section>

  <script
    type="application/ld+json"
    is:inline
    set:html={stringifyJsonLd({
      '@context': 'https://schema.org',
      '@type': 'HowTo',
      '@id': 'https://biokea.ai/pipeline#how-to',
      name: 'BioKEA six-stage sample-to-claim pipeline',
      description:
        'The BioKEA pipeline transforms raw field samples into verifiable, FAIR-compliant scientific artifacts across six stages.',
      step: pipelineStages.map((s, i) => ({
        '@type': 'HowToStep',
        position: i + 1,
        name: s.title,
        text: s.body,
      })),
    })}
  />

  <section class="max-w-6xl mx-auto px-6 py-12">
    <figure class="max-w-[840px] mx-auto">
      <img
        src="/assets/images/field-sampling.jpg"
        alt="An editorial illustration of a field scientist collecting a water sample beside a creek."
        width="1800"
        height="1200"
        class="w-full block rounded-sm"
        loading="lazy"
      />
      <figcaption
        class="mt-3 text-center font-mono text-[10px] tracking-[0.14em] uppercase text-slate-500"
      >
        Fig · where every claim begins — a field sample
      </figcaption>
    </figure>
  </section>

  <CtaBand
    title="Want to plug a sample into this?"
    subtitle="We're onboarding sample streams and collaboration partners."
    cta={{ href: '/contact', label: 'Start a conversation' }}
  />
</BaseLayout>
