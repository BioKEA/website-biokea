name = "biokea"
compatibility_date = "2026-04-18"

# Astro's Cloudflare adapter emits the worker entry + static assets;
# wrangler deploy picks them up from dist/ via `astro build`.

[vars]
CONTACT_FROM_EMAIL = "hello@biokea.ai"
CONTACT_TO_EMAIL = "team@biokea.ai"

# Secrets — set once per environment:
#   wrangler secret put RESEND_API_KEY
#   wrangler secret put TURNSTILE_SECRET_KEY   # optional; enables captcha
#
# If TURNSTILE_SECRET_KEY is unset, the contact endpoint skips captcha
# verification. Pair it with PUBLIC_TURNSTILE_SITE_KEY at build time so
# the widget renders on the contact page.
